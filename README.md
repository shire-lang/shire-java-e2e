# Shire Movie Project Demo

## Setup

1. clone this project and open in Intellij
2. install `Shire` plugin in Intellij Marketplace
3. Create `.env.shireEnv.json` to config your api keys

example:

```json
{
  "development": {
    "singleStoryKey": "xxxx",
    "epicStory": "xxxx"
  }
}
```
